import React from "react";
import { ProgressBar } from "../SubNavDominance/SubNavDominance.styles";

export const PortfolioProgressBar = ({ innerBar }) => {
  return <ProgressBar>{innerBar}</ProgressBar>;
};
